# Asset Invariants

- Source HF files are inputs, not the runtime layout.
- `manifest.blok` is the minimum contract before payload reads.
- Model bytes move by explicit O_DIRECT/uGDS/GDS paths only.
- Tokenizer metadata is not inference.
