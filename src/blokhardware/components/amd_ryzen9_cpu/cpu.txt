The CPU should not remain idle. It should take over orchestration, preprocessing, scheduling, memory
preparation, and runtime bookkeeping so the GPU can stay focused on matrix multiplications.

Many runtime algorithms will execute on the CPU. If the CPU prepares data, organizes batches,
manages transfers, and keeps queues full, the GPU can remain focused on sustained computation.
