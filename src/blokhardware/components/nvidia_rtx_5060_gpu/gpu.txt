We need to keep the GPU statically saturated at all times. GPU work should be organized so kernels remain compute-bound, queues stay full, and host-side stalls are minimized.

GPUDirect Storage should be implemented where supported. PTX should be used for low-level GPU control when necessary, keeping the execution path as close to the hardware as practical while still operating within the Linux and CUDA stack.
